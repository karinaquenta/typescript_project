
import User from './User'
import Shop from './Shop'
import Item from './Item'
// import { v4 as uuidv4 } from 'uuid';

export {
    Item,
    User,
    Shop
}
// const egg = new Item((uuidv4)(), 'Eggs','Organic Eggs',5)

// function driveCart(userName:string, age:number) {
//     const user1 = new User(( uuidv4(), userName, age, [])
//     const shop = new Shop (egg)
//     user1.cart.push(shop.ItemA);
//     user1.cartTotal(user1);
//     user1.printCart(user1);
//     user1.removeQuantityFromCart(user1, shop.itemA,1);
//     user1.cartTotal(user1);
//     user1.printCart(user1);
//     user1.removeFromCart(user1, shop.ItemA);


// const shop = new Shop()


// const user = new User ('Sebastian', 21)

// const itemA = shop.items[0]
// const itemB = shop.items[1]
// const itemC = shop.items[2]

// user.addToCart(itemA)
// user.addToCart(itemB)
// user.addToCart(itemC)

// user.printCart()

// user.removeFromCart(itemA)

// const quantityToRemoveFromCart = 1
// user.removeItemFromCart(itemB, quantityToRemoveFromCart)

// user.printCart()

